import ContentLayout from "@/components/content-layout/client";
import { PageProps } from "@/types/pageProps";

export default function ExcosPage({params:{lng}}:PageProps) {
    return(
        <ContentLayout lng={lng} title="excos">
            <div>
                
                </div>
        </ContentLayout>
    );
}