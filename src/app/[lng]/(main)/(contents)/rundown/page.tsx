import ContentLayout from "@/components/content-layout/client";
import { PageProps } from "@/types/pageProps";

export default function RundownPage({params:{lng}}:PageProps) {
    return(
        <ContentLayout lng={lng} title="rundown">
                       <div>
                
                </div>
        </ContentLayout>
    );
}