import ContentLayout from "@/components/content-layout/client";
import { PageProps } from "@/types/pageProps";

export default function GroupsPage({params:{lng}}:PageProps) {
    return(
        <ContentLayout lng={lng} title="groups">
                        <div>
                
                </div>
        </ContentLayout>
    );
}