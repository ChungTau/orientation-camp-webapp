import ContentLayout from "@/components/content-layout/client";
import { PageProps } from "@/types/pageProps";

export default function ManagemGroupPage({params:{lng}}:PageProps) {
    return(
        <ContentLayout lng={lng} title="ug-groups">
                        <div>
                
                </div>
        </ContentLayout>
    );
}