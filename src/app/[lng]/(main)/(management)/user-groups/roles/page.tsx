import ContentLayout from "@/components/content-layout/client";
import { PageProps } from "@/types/pageProps";

export default function ManageRolesPage({params:{lng}}:PageProps) {
    return(
        <ContentLayout lng={lng} title="ug-roles">
                        <div>
                
                </div>
        </ContentLayout>
    );
}